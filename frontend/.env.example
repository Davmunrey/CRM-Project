# Velo CRM — frontend env vars

# API base URL
# Local dev: http://localhost:3001
# Docker full-stack: omit (nginx proxies /api/* → api:3001, Vite defaults to /api)
VITE_API_URL=http://localhost:3001
