name: Build & publish Docker image

on:
  push:
    branches: [master]
    paths:
      - 'frontend/**'
      - 'api/**'
      - '.gitea/workflows/build-production.yml'
  workflow_dispatch:

env:
  IMAGE: gitea-clovrlabs.apps.privateprompt.tech/clovrlabs/n0crm-api

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout
        env:
          GIT_TOKEN: ${{ github.token }}
        run: |
          rm -rf .git
          git init
          git remote add origin "https://oauth2:${GIT_TOKEN}@gitea-clovrlabs.apps.privateprompt.tech/clovrlabs/n0CRM.git"
          git fetch --depth=1 origin "${GITHUB_SHA}"
          git checkout FETCH_HEAD

      - name: Log in to registry
        run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login gitea-clovrlabs.apps.privateprompt.tech -u "${{ secrets.REGISTRY_USER }}" --password-stdin

      - name: Build and push
        run: |
          docker build \
            --tag ${{ env.IMAGE }}:web \
            --tag ${{ env.IMAGE }}:web-${{ github.sha }} \
            --build-arg VITE_APP_CHANNEL=production \
            --file frontend/Dockerfile \
            .
          docker push ${{ env.IMAGE }}:web
          docker push ${{ env.IMAGE }}:web-${{ github.sha }}
