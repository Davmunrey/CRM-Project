/** Commercial product identity (Velo). */
export const APP_NAME = 'Velo'
export const APP_TAGLINE = 'Outbound-native CRM'

/**
 * Legacy persisted workspace titles compacted (`appName.replace(/\s+/g, '').toLowerCase()`).
 * Pre-rebrand installs may still have the old commercial product title in local settings.
 */
export const LEGACY_COMPACT_APP_TITLE_BEFORE_REBRAND = 'crmpro' as const
